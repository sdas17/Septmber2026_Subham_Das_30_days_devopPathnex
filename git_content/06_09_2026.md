#git is version control system is used for tracking code 

git pull 
git clone 
git checkout -b 
git branch
git switch 
git add
git push origin
git commit -m "copssds"
git branch 
git checkout -b pathnext

fetch → see/download first
pull  → download + integrate


.git
│
├── HEAD       → current branch/position
├── index      → staging area
├── objects    → Git data (blob, tree, commit)
├── refs       → branches/tags
├── config     → Git repository settings
└── logs       → reference history
