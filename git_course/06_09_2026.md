git init
git add .
git commit -m "push they code"
git push

git branch
git switch 
git checkout -b branch name

git fork is clone of code 
git pull and git fetch is using 
git pull is change current code 
git fetch is download new changes

git clone  